{
  "name": "workflow-examples",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsc -p tsconfig.json && node build/index.js",
    "build": "npx tsc -p tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^2.2.14",
    "@types/express": "^4.17.21",
    "@types/node-cron": "^3.0.11",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
